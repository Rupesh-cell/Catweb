const {createSlice} = require('@redux/toolkit');

const initialState = [];

const cartSlice = createSlice({
    name: 'cart',
    initialState,
    reducers: {
        add() {},
        delete() {},
        update(jn gbnv bn )
    },

})
//jdmfn
